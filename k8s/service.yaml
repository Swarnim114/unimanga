apiVersion: v1
kind: Service
metadata:
  name: unimanga-backend
  namespace: default
  labels:
    app: unimanga-backend
spec:
  type: NodePort
  selector:
    app: unimanga-backend
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30001
    protocol: TCP
    name: http
